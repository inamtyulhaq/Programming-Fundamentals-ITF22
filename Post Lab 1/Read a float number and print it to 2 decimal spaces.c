#include <stdio.h>
int main(){
    float num;
    printf("Write your floating-point number: ");
    scanf("%f", &num);
    printf("The number with two decimal places will be %.2f", num);
    return 0;
}

